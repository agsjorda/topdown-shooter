using UnityEngine;

public class LeftHandTargetTransform : MonoBehaviour {
    //We need this script to make sure the left hand target transform follows the gun's left hand target position and rotation
}
